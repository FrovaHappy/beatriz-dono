---
import getPublic from "@utils/getPublic";
import Settings from "../../components/Dashboard/Guild/Settings";

import Layout from "../../layouts/Layout.astro";

const { urlApi, urlOathCallback } = getPublic(import.meta.env.PUBLIC_VARIABLES);
---

<Layout title="Guild Management">
  <h1>Guild Management</h1>
  <Settings apiUrl={urlApi} oauthCallback={urlOathCallback} client:only />
</Layout>
